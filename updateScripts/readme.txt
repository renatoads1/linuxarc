Os arquivos que conterão os scripts devem ser chamados da seguinte maneira:

[major]-[minor]-[release]-[build].sql

O sistema ira verificar a versão do banco e do sistema e comparar, e a partir daí irá abrir os arquivos .sql e executar os scripts.

Exemplo de nome: 1-2-0-500.sql
